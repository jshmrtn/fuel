---

fuel_local_url: your-local-url.dev
fuel_local_site_path: /path/to/your/local/project/site
fuel_local_vagrant_provider: your_vm_provider # vmware_fusion / virtualbox / vmware_workstation / parallels

fuel_slack_username: ''

fuel_use_vm: true

fuel_packagist_username: ''
fuel_packagist_token: ''

# Things to replace (If you use this, the default site replace will not run!)
# fuel_replace:
#     # The Site when importing from remote
#   - site_local: "{{ fuel_remote_url }}/wp"
#     # The site when exporting from local
#     site_remote: "{{ fuel_local_url }}/wp"
#     # Replacements
#     replace:
#         # The Old String (New String on remote)
#       - old: "{{ fuel_remote_url }}"
#         # The New String (Old String on remote)
#         new: "{{ fuel_local_url }}"
